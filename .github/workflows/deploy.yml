
name: 'Deploy Next.js App'

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: 'Build'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}

      - name: Create package
        run: |
          tar -czf deploy.tar.gz \
            --exclude='.next/cache' \
            .next public package.json package-lock.json next.config.ts prisma

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: deploy-package
          path: deploy.tar.gz

  deploy:
    name: 'Deploy'
    runs-on: ubuntu-latest
    needs: build          # waits for build to finish
    environment: prod     # ← this triggers manual approval
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: deploy-package

      - name: Ensure Remote Directory Exists
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > key.pem
          chmod 600 key.pem
          ssh -i key.pem -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.AWS_EC2_IP }} "mkdir -p /home/ec2-user/app"
          rm key.pem

      - name: Copy to EC2
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > key.pem
          chmod 600 key.pem
          scp -i key.pem -o StrictHostKeyChecking=no deploy.tar.gz ${{ secrets.SSH_USERNAME }}@${{ secrets.AWS_EC2_IP }}:/home/ec2-user/app/
          rm key.pem
          
      - name: Finalize on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_EC2_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          timeout: 60s
          command_timeout: 15m
          script: |
            # 1. Ensure Node.js 22 is installed before proceeding
            NODE_VERSION=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
            if [ "$NODE_VERSION" -lt "22" ]; then
              echo "Node version is $NODE_VERSION. Upgrading to Node 22..."
              curl -fsSL https://rpm.nodesource.com/setup_22.x | sudo bash -
              sudo yum install -y nodejs
            fi

            set -e
            cd /home/ec2-user/app
            
            # 2. Cleanup and Extraction
            pm2 delete nextjs-app || true
            sync && sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches' || true
            tar -xzf deploy.tar.gz
            rm deploy.tar.gz

            # 3. Smart Install
            OLD_HASH=$(cat .pkg_hash 2>/dev/null || echo "")
            NEW_HASH=$(sha256sum package.json | awk '{print $1}')
            if [ "$NEW_HASH" != "$OLD_HASH" ]; then
              export NODE_OPTIONS="--max_old_space_size=384"
              # Now running on Node 22, so EBADENGINE won't trigger
              npm ci --omit=dev --prefer-offline --no-audit --no-fund
              echo "$NEW_HASH" > .pkg_hash
            fi

            # 4. Environment and Execution
            echo "DATABASE_URL=postgresql://${{ secrets.DB_USERNAME }}:${{ secrets.TF_VAR_db_password }}@${{ secrets.DB_HOST }}:5432/${{ secrets.DB_NAME }}" > .env
            echo "NEXT_PUBLIC_API_URL=https://neosinternalprojects.dpdns.org" >> .env
            echo "SYSTEM_ADMIN=${{ secrets.SYSTEM_ADMIN }}" >> .env
            echo "SYSTEM_ADMIN_PASSPWORD=${{ secrets.SYSTEM_ADMIN_PASSPWORD }}" >> .env
            
            node --env-file=.env prisma/seed.mjs
            pm2 start npm --name "nextjs-app" -- start -- -H 0.0.0.0
            pm2 save
